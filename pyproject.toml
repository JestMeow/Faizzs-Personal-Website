[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "faizzs-website"
version = "0.1.0"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
]

